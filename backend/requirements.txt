fastapi==0.115.6
uvicorn[standard]==0.34.0
sqlalchemy==2.0.36
alembic==1.14.0
psycopg[binary]==3.2.3
pydantic==2.10.4
pydantic-settings==2.7.0
python-jose[cryptography]==3.3.0
# bcrypt>=4.1은 passlib 1.7.4가 참조하는 `bcrypt.__about__.__version__`을 제거해 호환이 깨짐
# (passlib 최신 릴리스도 아직 미수정) — 4.0.x로 고정
passlib[bcrypt]==1.7.4
bcrypt==4.0.1
redis==5.2.1
openpyxl==3.1.5
python-multipart==0.0.20

# pytest는 테스트 전용 의존성 (Phase 3 "완료 기준" — Pytest 단위 테스트).
# httpx는 테스트(TestClient) 외에도 AI Chat(app/services/ai_chat.py)의 LLM API 호출에
# 사용한다 — Phase 6, 로드맵 §8 "AI Chat 화면 구현"
pytest==8.3.4
httpx==0.28.1
